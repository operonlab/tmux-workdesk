# Editor / OS cruft
.DS_Store
*.swp
*~

# tmux-ide keeps no runtime state on disk — the layout lives entirely in the
# running tmux server (windows and panes), so there is nothing under
# ${TMUX_TMPDIR:-/tmp}/ or here to ignore.

# Demo build intermediates (vhs raw capture pre-overlay; agent runtime state)
docs/demo-raw.gif
.omx/

# Python bytecode from docs/keycast-bezel.py
docs/__pycache__/

# Local worktrees
.worktrees/
